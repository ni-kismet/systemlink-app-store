{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ni/systemlink-app-store/app-manifest.schema.json",
  "title": "SystemLink App Store Submission Manifest",
  "description": "Schema for manifest.json files in submissions/ directories. Used by CI to validate app submissions.",
  "type": "object",
  "required": [
    "package",
    "version",
    "displayName",
    "description",
    "section",
    "maintainer",
    "license",
    "appStoreCategory",
    "appStoreType",
    "appStoreAuthor"
  ],
  "properties": {
    "package": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._-]*$",
      "minLength": 3,
      "maxLength": 100,
      "description": "Unique package identifier (lowercase, hyphens/dots/underscores allowed). First-come-first-served."
    },
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$",
      "description": "Semantic version (MAJOR.MINOR.PATCH). No pre-release or build metadata."
    },
    "displayName": {
      "type": "string",
      "minLength": 3,
      "maxLength": 200,
      "description": "Human-readable app name shown in the store UI."
    },
    "description": {
      "type": "string",
      "minLength": 20,
      "maxLength": 5000,
      "description": "Multi-line description of the app."
    },
    "section": {
      "type": "string",
      "enum": ["WebApps", "Notebooks", "Add-Ons"],
      "description": "Top-level category for filtering."
    },
    "maintainer": {
      "type": "string",
      "minLength": 3,
      "description": "Author name and email, e.g. 'Acme Corp <apps@acme.com>'."
    },
    "homepage": {
      "type": "string",
      "format": "uri",
      "description": "Link to project homepage or documentation."
    },
    "license": {
      "type": "string",
      "minLength": 2,
      "description": "SPDX license identifier (e.g. MIT, Apache-2.0) or 'Proprietary'."
    },
    "appStoreCategory": {
      "type": "string",
      "minLength": 2,
      "description": "Fine-grained category: Dashboard, Data Analysis, Integration, Monitoring, etc."
    },
    "appStoreType": {
      "type": "string",
      "enum": ["webapp", "notebook", "routine", "bundle"],
      "description": "Resource type installed."
    },
    "appStoreAuthor": {
      "type": "string",
      "minLength": 2,
      "description": "Display author name shown in the store UI."
    },
    "appStoreTags": {
      "type": "string",
      "description": "Comma-separated search tags."
    },
    "appStoreRepo": {
      "type": "string",
      "format": "uri",
      "description": "Source code repository URL."
    },
    "appStoreMinServerVersion": {
      "type": "string",
      "description": "Minimum SystemLink server version, e.g. '2024 Q4'."
    },
    "nipkgFile": {
      "type": "string",
      "pattern": "\\.nipkg$",
      "description": "Filename of the .nipkg in this submission directory."
    }
  },
  "additionalProperties": false
}
